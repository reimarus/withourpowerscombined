{
  "rules": {
    "games": {
      ".read": true,
      "$game_id": {
        ".write": true,
        ".validate": "newData.hasChildren(['game_name','host_name','map_name','player_count','max_players','lobby_port','host_ip','last_seen'])"
      }
    },
    "reports": {
      ".read": true,
      "$report_id": {
        ".write": true,
        ".validate": "newData.hasChildren(['timestamp','host_name','log','version'])"
      }
    },
    "logs": {
      ".read": true,
      "$log_id": {
        ".write": true,
        ".validate": "newData.hasChildren(['event','timestamp'])"
      }
    }
  }
}
