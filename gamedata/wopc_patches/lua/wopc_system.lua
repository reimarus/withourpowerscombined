-- =============================================================================
-- wopc_system.lua
-- The initialization entry point for the WOPC custom Lua overlay.
-- =============================================================================

function Init()
    LOG("======================================================")
    LOG("================ WOPC OVERLAY ACTIVE =================")
    LOG("======================================================")
    LOG("WOPC: Booting custom Lua systems and compatibility hooks.")
end

-- Automatically execute when this file is loaded
Init()
