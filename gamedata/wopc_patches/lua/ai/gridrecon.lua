-- gridrecon.lua — WOPC stub
-- FAF's AI brains import this during OnBeginSession. Only exists in
-- FAF's distribution build (not in source repo or vanilla lua.scd).
-- All callers store the return value and nil-check before use.

function Setup(aiBrain)
    -- no-op: return nil (callers nil-check before use)
    return nil
end
